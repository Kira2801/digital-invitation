import { Hero, Couple, Gallery, Event, RSVP } from './elegant';

export const themes = {
  elegant: {
    name: 'Elegant',
    description: 'A sophisticated and romantic wedding theme with warm amber tones',
    components: {
      Hero,
      Couple,
      Gallery,
      Event,
      RSVP
    }
  }
};

export default themes;
